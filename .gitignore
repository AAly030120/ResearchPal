# ── Environment & Secrets ──
.env
.env.local
.env.production
.api_keys.json
*.db

# ── Python ──
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
backend/venv/
backend/.venv/
*.egg

# ── Node ──
node_modules/
frontend/.next/
frontend/out/

# ── OS & IDE ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ── Uploads & Outputs (dev) ──
backend/uploads/*
!backend/uploads/.gitkeep
backend/outputs/*
!backend/outputs/.gitkeep

# ── PPT master cache (optional skill, ~80MB; PPT falls back to python-pptx) ──
/skills/
backend/skills/

# ── RAG vector store (Chroma, runtime-generated) ──
backend/chroma_store/
chroma_store/

# ── ppt-master build probes (temp) ──
backend/.pptx-build-*/

# ── Local dev: auth bootstrap used only for tooling screenshots ──
frontend/public/_auth*.html
backend/tmp_*.py

# TypeScript build info
frontend/tsconfig.tsbuildinfo

# ── Local-only interview prep / resume drafts (NEVER push — for candidate's eyes only) ──
docs/INTERVIEW_LIFECYCLE.md
docs/INTERVIEW_LIFECYCLE_V2.md
docs/*_PREP.md
docs/RESUME_RESEARCHPAL.md
*INTERVIEW_PREP*
